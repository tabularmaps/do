# tabularmaps / 北海道 16×16 tabular map — 設計規約 (現在形)

このファイルは「今なにが正しいか」だけを書く。経緯・却下案・証拠は `DECISIONS.md` (追記専用) に書く。
英語の引き継ぎ原文は `prototypes/v06/CLAUDE-handoff-en.md` に保存してある (歴史的資料、現在の規約はこちら)。

## 使命

tabularmaps は、地理的な忠実さよりも「一望性」と「安定した比較」を優先するダッシュボード向けの地図。
北海道の 179 市町村を固定の 16×16 グリッドに圧縮する。一望性・比較・ラベル・ダッシュボード利用が
良くなるなら、地理は歪めてよい。

これは通常の地図でも、選挙区カルトグラムでも、人口比例カルトグラムでも、市町村の格付けでもない。

## 現在の基準 (v08)

- グリッド: 16×16 = 256 セル。
- 札幌: 4×4 (16 セル)。
- 広域拠点 10 か所: 2×2 (函館・小樽・旭川・稚内・北見・苫小牧・室蘭・帯広・釧路・根室)。
- その他 168 市町村: 1×1。
- 1×2 / 2×1 は合法だが未使用。
- 構造余白 32 セル: 無名で、意図的に残す。うち東端の列 (x=15) の 6 セルは北方領土の 6 村の席 (下記)。
- 179 市町村がちょうど 1 回ずつ現れる。地図の既定表示は根室振興局管内の 6 村 (北方領土) を含む (D18)。

```text
179 市町村の基本セル
+15 札幌 4×4 の追加分
+30 2×2 拠点 10 か所の追加分
=224 占有セル
256 − 224 = 32 構造余白 (= 26 + 北方領土の 6 村の席 6)
```

32 セルの余白は「あるから使う」ものではない。

## 北方領土の 6 村の扱い

事実 (一次情報は `DECISIONS.md` D16):

- 北方領土 (歯舞群島・色丹島・国後島・択捉島) は根室振興局管内。歯舞群島は根室市の一部である
  (旧歯舞村は昭和 34 年 4 月 1 日に根室市へ編入)。
  色丹島・国後島・択捉島には 色丹村・泊村・留夜別村・留別村・紗那村・蘂取村 の 6 村がある。
- 6 村は地方自治法上の地方公共団体として存置され、全国地方公共団体コード (01695〜01700) を持つ。
  一方、現実に行政権を行使し得ない状態にあり村長も置かれておらず、市町村としての行政の実態がない
  (内閣衆質九五第五号答弁書、昭和 56 年 11 月 6 日)。
- 政府の慣行: 北海道庁は「道内 179 市町村」、総務省の市町村数は 179 (35 市 129 町 15 村) で「北方領土の 6 村を
  含めると」と注記、総務省のコード一覧と国土地理院の面積調は 6 村を収録する (面積調の北海道は 185 市町村)。

このリポジトリの扱い (政府の慣行に合わせる):

- 地図としては 6 村を既定で描く (地理院地図・全国市町村要覧の地図・国土数値情報と同じ扱い)。市町村数の集計は
  179 (北海道庁・総務省の市町村数と同じ範囲) で、`board.csv`・`counts.active`・文中の「179 市町村」はこれに従う。
- 6 村は `status: "northern_territories"` を付けてデータに保持し、`includeNorthernTerritoriesVillages`
  (既定 true、UI 表記「北方領土の 6 村を含める / 含めない」) を false にすると 179 市町村だけの表示になる。
  含めない時、その 6 セルは構造余白と同じ見た目にする。含める時は他の 1×1 と同じ見た目で描き、視覚的な区別は付けない (状態はツールチップと表の文字情報で示す)。
- 6 村は東端の列 (x=15) の 1×1 にだけ置く (`validate_layout.py` が強制)。含めない表示で凹みを作らないため。
- 6 村の表示名は正式名称 (「泊村」など)。後志の泊村と区別するため。
- 表記は政府の公表資料の言い回しに揃える: 「北方領土の 6 村」「根室振興局管内」「市町村としての行政の実態がない」
  「歯舞群島は根室市の一部」。領土の帰属や 6 村の状態についてこれ以外の表現 (例: 「施政」を使った言い回し) をしない。海のセルに名前を付けない
  (海峡も同じ)。「北方領土を含む/含まない」とは書かず、含める対象は常に「6 村」とする。
  「北海道 179 市町村 + 北方領土の 6 村」のように 179 と 6 村を並列に足す表現は、北方領土が北海道の外にあるように
  読めるので使わない (D19)。6 村に触れる時は「根室振興局管内の 6 村」のように北海道の内側に位置づける。

拠点 10 か所の一覧は作業仮説であり、変えるには全道の配置を比較した上で明示的に決める。

## 階級を 4×4 / 2×2 / 1×1 に限る理由

初期の試作は 1×2 / 2×1 の連結セルを多用したが、(1) 中間階級に候補が集まりすぎる、
(2) 行政上の地位・人口・交通機能・地域アイデンティティが 1 つの視覚変数に混ざる、
(3) 向きが意図しない意味を生み、詰め込みの自由度を下げる、という問題が出た。

1×1 から 1×2 / 2×1 への昇格は、完成した基準案が具体的な問題を露わにし、昇格がそれを解く時にだけ行う。
認められる理由: 地域の連続性が実質的に改善する / 隣の拠点に従属して見える誤解を防ぐ /
離れた 2 クラスタの本物の橋を表す / 恒常的なラベル・詰め込み問題を解く / スタイルでは扱えない頻出比較を支える。
それだけでは不十分な理由: 市であること / 振興局の所在地であること / 面積が大きいこと / 有名であること /
人口順位が相対的に高いこと。昇格は余白 1 セルを消費し、必ず解く問題を明記する。

## 構造余白

構造余白は設計文法の一部であり、失敗の残りではない。用途: 混同されやすいクラスタの分離 /
地域の継ぎ目の可読化 / 大ブロック周りの圧力の緩和 / 将来の昇格余力 / 題字・凡例・外部接続の支え。

湾・海などの地物名を余白セルに付けない (「石狩湾」セルは却下済み)。輪郭をなぞるために余白を使わない。
v07 の余白の役割一覧は `DECISIONS.md` D8。

## 配置の優先順位

1. 179 市町村を 1 回ずつ含める。
2. 各市町村のフットプリント階級を守る。
3. コンパクトなダッシュボード的構図を保つ。
4. 大まかな南北・東西関係を認識できるようにする。
5. 機能的な地域クラスタ (振興局) を読めるようにする。
6. 選ばれた高価値の隣接を保つ。
7. 構造余白は識別できる仕事をする場所にだけ置く。
8. 以上を満たした後で美観を整える。

厳密な行政境界の隣接は要求しない。

## 設計知識 (v07–v08 で採用)

- 小樽は札幌の左上 (北西) に読める位置。積丹半島は小樽の北に伸びる。
- 石狩平野は 札幌→江別→岩見沢→美唄→砂川→滝川→深川→旭川 の鎖として読める。
- 江別・恵庭・千歳・北広島は 1×1 のまま (札幌の東列)。昇格は必要性が示されるまで行わない。
- 振興局の所在地は自動的に大きくならない (浦河・江差・倶知安などは 1×1)。
- 北見・網走・紋別、釧路・根室・中標津、苫小牧・室蘭 はそれぞれ系として判断する。
- 渡島半島は 2 列の帯に「展開」する: 上段 = 噴火湾沿い (長万部→八雲→森→鹿部→七飯→函館)、
  下段 = 津軽海峡沿い (松前→福島→知内→木古内→北斗→函館)。函館は帯の東端。
- 根室は南東端、北に別海・中標津・標津・羅臼。釧路は根室の南西、東に釧路町・厚岸・浜中。
- オホーツク東岸は x=14 の列に 網走→小清水→斜里→清里 と北から並び、羅臼 (根室) につながる。
- 東端の列 (x=15) は北から 蘂取村・紗那村・留別村 (択捉島)、海峡、留夜別村・泊村 (国後島)、海峡、色丹村、
  別海、根室。6 村を含めない表示では根室より北は全て余白として読める。

## メタデータとフットプリントの分離

すべての重要な属性を面積に符号化しない。振興局所在地・人口順位・市町村区分・港湾/空港/医療/物流の機能・
境界/離島/僻地の役割・ダッシュボードの統計値は、メタデータまたは任意のスタイル (枠・アイコン・ラベル・
パターン・ツールチップ・操作状態) で表す。**セルの塗り色はダッシュボードのデータが自由に使えるように空けておく**。
振興局の色は「無データ」時の既定表示にだけ使う。

## 札幌の展開

札幌は 4×4 の外形を不変に保ち、区データが必要な時だけ 10 区に展開する (`data/sapporo-wards.json`)。
区は市町村ではなく札幌市の子要素。全区を矩形で表し、セル数は札幌市の推計人口 (令和 8 年 9 月 1 日現在) を 16 セルに
最大剰余法で按分した値 (中央2 北2 東2 白石2 豊平2 西2、手稲・南・厚別・清田 各1) とする (D20)。方位は概略のみ保つ。

## リポジトリ構成

```text
data/municipalities.json   北海道の市町村マスター (179 市町村と根室振興局管内の 6 村を status で区別。コード・振興局・区分・役場の概略座標・階級)
data/layout-v08.json       現行の配置 (一次データ、long-form)
data/board.csv             8bit 互換の 16×16 派生表現 (179 市町村)。board-with-villages.csv は 6 村込み
data/sapporo-wards.json    札幌 10 区の内部配置
design/territories-v08.txt 振興局の領土図 (人手で描く 16 行×16 文字、X = 6 村の席)
scripts/generate_layout.py 配置生成器 (領土図 + 拠点・ピン + 領土内の最適化)
scripts/validate_layout.py 機械検証 (CI で回す対象)
scripts/render_layout.py   レビュー用 PNG
scripts/export_board_csv.py, scripts/count_territories.py
prototypes/vNN/            各版の PNG と引き継ぎ資料 (前版は消さない)
docs/                      GitHub Pages: Open MCT ダッシュボード (index.html) と単体プレビュー (preview.html)
```

## 配置データの契約 (layout-*.json)

- `placements`: `[{code, name, bureau, status, x, y, w, h}]` が一次表現。`code` は全国地方公共団体コード上 5 桁、
  `(x, y)` は左上セル (原点は左上)、`w×h` はフットプリント、`status` は `active` (179) か `northern_territories` (6 村)。
- `structural_spaces`: `[[x, y], ...]` 26 セル (無名だが一次データ)。6 村の席は `northern_territories.cells`。
- `counts`: `{active: 179, northern_territories: 6}`。
- `board`: 16×16 の名前配列 (派生、179 市町村、空白は `''`)。`board_all` は 6 村込み。
- `generator`: `{script, territories, seed, metrics}`。
兄弟プロジェクト tabularmaps/cldr とこのキー名を共有する。

## 新しい版を作る手順

1. フットプリント階級を凍結する (`data/municipalities.json` の `footprint`)。
2. `design/territories-vNN.txt` をコピーして領土図を描き直す。`scripts/count_territories.py` で収支を合わせる。
3. `scripts/generate_layout.py` の `ANCHOR_HINTS` (大ブロック) と `PINS` (設計上固定する 1×1) を更新する。
   ピンには必ず理由コメントを付ける。
4. `python3 scripts/generate_layout.py --version vNN` → `python3 scripts/validate_layout.py data/layout-vNN.json`
   → `python3 scripts/render_layout.py data/layout-vNN.json prototypes/vNN/….png` (6 村込みは `--with-villages`)。
5. 全体を見てから 1×2 昇格を検討する。昇格・降格は理由とともに `DECISIONS.md` に列挙する。
6. 各構造余白が仕事をしているか確認する。
7. `docs/data/` に配置データを複製し、`docs/openmct-plugin.js` の参照ファイル名を更新する。

最適化器の役割は「領土の中の並び順」だけ。振興局の継ぎ目と余白の位置は目的関数に任せず人が描く (D4)。

## Open MCT との統合

`docs/openmct-plugin.js` は `objects.addProvider` + `composition.addProvider` + `objectViews.addProvider` だけを使う。
Telemetry API (request/subscribe) と Display Layout は使わない (dwg7/cafebabe の Open MCT ノウハウに従う)。
指標は `sources: [{key, name, refreshMs, fetchValues() | valuesUrl}]` で渡し、`{label, unit, asOf, min, max,
values: {<code>: number}}` を返す。`includeNorthernTerritoriesVillages` (既定 true) で 6 村の初期表示を決める。
指標は任意で `notes` (市町村ごとの説明文) と `scale: {type: 'ordinal', labels, colors?}` (順序尺度。`colors` は段階ごとの
`#rrggbb` で、組み込み先の配色に揃える時に使う) を持てる。
実データの指標は `docs/jma-warnings.js` (気象庁の気象警報・注意報、r8 形式の JSON を 8 府県予報区分合算) が最初の例。
気象庁の旧形式 `warning/data/warning/` は更新が止まっているので使わない (D21)。
他のダッシュボードに組み込む時は `TabularMapsJmaWarnings.create({fetchJson, codesUrl, codeTable, colors})` を使う
(取得の差し替え・コード表の場所・配色。dwg7/sas0 が採択、D22)。Open MCT は unpkg の 4.3.1 に固定 (dist/openmct.js と espressoTheme.css の
実在を確認済み)。CDN 経由なので `window.SharedWorker = undefined` を先に置く。

## 一次情報源

- 北海道庁「総合振興局・振興局別市町村」 https://www.pref.hokkaido.lg.jp/link/shichoson/
  https://www.pref.hokkaido.lg.jp/gyosei/shicho/index.html
- 総務省「都道府県コード並びに市区町村コード」 https://www.soumu.go.jp/denshijiti/code.html (照合は `scripts/check_codes.py`)
- 総務省「市町村数」 https://www.soumu.go.jp/kouiki/kouiki.html
- 国土地理院「全国都道府県市区町村別面積調」 https://www.gsi.go.jp/KOKUJYOHO/MENCHO-title.htm
- 札幌市 区政概要・人口統計 https://www.city.sapporo.jp/shimin/shinko/kusei-suishin/gaiyo/index.html
  https://www.city.sapporo.jp/toukei/jinko/jinko.html
- `data/municipalities.json` の座標は役場所在地の概略値 (±0.05° 程度) で、配置の目安にのみ使う。

## 応答・記述の言語

会話は日本語 (敬語)。リポジトリのドキュメント・コメントも日本語。コード識別子は英語。
